上来问项目（问的是嵌入式的，感觉已经凉了），虚函数表是与谁绑定的，存在于哪里，是每一个对象都有一个虚函数表还是所有对象公用一个虚函数表，如何查看进程所占用的资源，Linux相关命令会哪些？以select，写一个服务器程序，listen函数放在哪里？accept函数放在哪里，sprintf snprintf区别，内存溢出，如何避免内存溢出，内存泄露，如何避免内存泄露，智能指针的原理，两种触发模式，select和epoll，如果一个进程被（Call？这个我不知道是啥，没听过）掉，如何去查看这个进程的啥啥啥，了解加密算法么（问的是对称加密，非对称加密这种，凉透了），http
